# Copy this file to keystore.properties and fill the values before building a signed bundle.
# Do NOT commit secrets into version control.
storeFile=android/app/my-release-key.keystore
storePassword=CHANGE_ME
keyAlias=my-key-alias
keyPassword=CHANGE_ME
